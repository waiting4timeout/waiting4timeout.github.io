# Place sound or model assets here (optional).
#
# The game ships with synthesized Web Audio SFX and generated geometry, so no
# assets are required to run. Drop .mp3/.wav/.glb files here if you want to
# swap in custom audio or 3D models later, then load them from the relevant
# system/entity (e.g. AudioManager or an entity constructor).
